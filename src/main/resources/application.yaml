spring:
  application:
    name: multicode_reader

  datasource:
    url: jdbc:postgresql://pg-84cd2b0-agustinmarino38-d89f.d.aivencloud.com:24486/defaultdb?sslmode=require
    username: avnadmin
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operationSorter: alpha