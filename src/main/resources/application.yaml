spring:
  application:
    name: multicode_reader

  datasource:
    url: jdbc:mariadb://localhost:3306/multicode_reader
    username: root
    password: agussuper34
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect